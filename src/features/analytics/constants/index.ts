/**
 * クライアントとサーバーの両方から参照する定数。
 *
 * "use client" が付いたモジュールをサーバー側から import すると、
 * コンポーネント以外の名前付きエクスポートは undefined になる。
 * 収集APIの検証（サーバー）とトラッカー（クライアント）が同じ値を見る必要があるので、
 * この種の定数はディレクティブを持たないこのファイルに置く。
 */

/** ページを高さで何等分して滞在を測るか */
export const REGION_BAND_COUNT = 10;
